media: media

content:
  - name: newsletter
    label: Newsletter
    type: collection
    path: newsletters
    view:
      fields: [title, date]
    fields:
      - name: title
        label: Title
        type: string
      - name: date
        label: Date
        type: date
      - name: body
        label: Body
        type: rich-text
